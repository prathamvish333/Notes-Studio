apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: notes-ingress
  namespace: notes-sit
spec:
  ingressClassName: nginx
  rules:
  - host: prathamvishwakarma.com
    http:
      paths:
      - path: /notes
        pathType: Prefix
        backend:
          service:
            name: notes-frontend-service
            port:
              number: 3000
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: notes-backend-ingress
  namespace: notes-sit
spec:
  ingressClassName: nginx
  rules:
  - host: prathamvishwakarma.com
    http:
      paths:
      - path: /api
        pathType: Prefix
        backend:
          service:
            name: notes-backend-service
            port:
              number: 8000
