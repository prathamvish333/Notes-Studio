apiVersion: apps/v1
kind: Deployment
metadata:
  name: notes-backend
  namespace: notes-dev
spec:
  replicas: 2
  selector:
    matchLabels:
      app: notes-backend
  template:
    metadata:
      labels:
        app: notes-backend
    spec:
      containers:
        - name: backend
          image: prathamvish333/notes-backend:dev
          imagePullPolicy: Always
          ports:
            - containerPort: 8000
          env:
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: notes-db-secret
                  key: database-url
